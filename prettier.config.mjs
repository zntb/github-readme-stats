/** @type {import("prettier").Config} */
const config = {
  trailingComma: "all",
  useTabs: false,
  endOfLine: "auto",
  proseWrap: "always",
  semi: true,
  singleQuote: false,
  printWidth: 100,
};

export default config;
